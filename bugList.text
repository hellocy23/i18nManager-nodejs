1.项目之间应该是独立的，不能相互覆盖

2.项目设置了几种语言，excel也就只能导入几种语言，item也显示几种语言。

3.编辑item的时候应该可以同时编辑多个语言，然后再提交，不要编辑一种语言提交一次。

4.搜索key的时候应该支持模糊搜索，而不是需要输入完整key值。

5.删除项目，同时把item里面对应的项目删掉。

6.新增项目导入之后，显示iterm如果这个语言，原来项目有的，新项目没有的，不会清空，而是继续显示。

7.导出项目的时候，根据这个项目设置的语言导出
